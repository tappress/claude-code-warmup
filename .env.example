# Required: Claude Code OAuth refresh token (seed value)
# Find it in: ~/.claude/.credentials.json → .claudeAiOauth.refreshToken
# Extract: cat ~/.claude/.credentials.json | python3 -c "import sys,json; print(json.load(sys.stdin)['claudeAiOauth']['refreshToken'])"
# Once set, rotated tokens are persisted automatically to Vercel KV (see below).
CLAUDE_REFRESH_TOKEN=sk-ant-rt01-...

# Optional: Custom warm-up message sent to Claude at the scheduled time
WARMUP_MESSAGE=

# Auto-generated by Vercel — locks the endpoint so only cron invocations are accepted.
# Do NOT set this manually; Vercel handles it automatically when crons are configured.
# CRON_SECRET=<auto>

# -----------------------------------------------------------------
# Vercel Redis (required — persists rotated refresh tokens across cron runs)
# -----------------------------------------------------------------
# Create a Redis store in your Vercel project dashboard → Storage → Create Database.
# Vercel auto-populates this env var when you link the store to your project.
REDIS_URL=
